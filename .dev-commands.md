# 🚀 VideoTool 开发快速命令

## 日常开发

```bash
# 启动开发环境（自动清理残留进程）
pnpm run dev

# 停止：在终端按 Ctrl+C
```

## 遇到问题时

```bash
# 1. 清理残留进程
pnpm run kill-dev

# 2. 完全重启（清理 + 重新构建）
rm -rf dist && pnpm run dev

# 3. 检查是否有残留进程
ps aux | grep VideoTool
```

## 其他命令

```bash
# 仅启动渲染进程
pnpm run dev:renderer

# 仅启动主进程
pnpm run dev:main

# 构建生产版本
pnpm run build

# 打包 macOS 应用
pnpm run build:mac
```

---

💡 **提示**: 如果 `Ctrl+C` 后仍有残留进程，只需运行 `pnpm run kill-dev`
📖 **详细文档**: 查看 `DEV_PROCESS_GUIDE.md`


## 问题
- 结果提示紧贴按钮，层次不清
- 成功提示配色偏亮，视觉对比不佳

## 改进方案
### 布局间距
- 在结果提示外增加容器，提供额外顶部间距：
  - 修改 `src/renderer/components/Features/SubtitleBurnTab.tsx:529-542`
  - 将当前结果提示包裹为：
    - `<div className={styles.resultSection}> ...现有 alert... </div>`
- 样式新增：
  - 在 `SubtitleBurnTab.module.scss` 添加：
    - `.resultSection { margin-top: t.$spacing-5; }`（比按钮间距更舒适）

### 成功提示配色
- 调整 `.alertSuccess` 的背景与边框，提高可读性：
  - `background-color: #F0FDF4`（更浅的绿，避免刺眼）
  - `border-color: #86EFAC`（柔和边框）
  - `color: #166534`（深绿文字，提高对比）
  - 可选：改为左侧强调线 + 中性背景（如 `background: var(--vt-color-surface)`，`border-left: 3px solid #22C55E`），若你更偏好商务风清爽样式。

### 保持一致性
- 不动其他警告/错误样式，仅优化成功提示
- 不改按钮组现有布局，单独靠结果容器控制间距

## 交付
1) 更新 TSX 包装容器（resultSection）
2) 在 SCSS 增加 `resultSection` 并优化 `.alertSuccess` 配色
3) 验证布局与对比度，确保与侧栏和卡片风格一致

确认后我立即实施修改并预览。